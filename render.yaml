services:
  - type: web
    name: credito-repasse
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: bash backend/render_start.sh
    envVars:
      - key: DATABASE_URL
        sync: false
